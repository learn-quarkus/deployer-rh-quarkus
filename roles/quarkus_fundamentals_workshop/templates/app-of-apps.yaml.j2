---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ quarkus_workshop_app_of_apps_gitops_app }}
  namespace: {{ quarkus_workshop_argocd_namespace }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: {{ quarkus_workshop_app_of_apps_gitops_app }}
    name: ""
  project: default
  syncPolicy:
    automated:
      prune: false
      selfHeal: false
    syncOptions:
      - ApplyOutOfSyncOnly=false
      - CreateNamespace=true
  source:
    repoURL: {{ quarkus_workshop_gitops_repo }}
    targetRevision: {{ quarkus_workshop_gitops_repo_tag }}
    path: {{ quarkus_workshop_app_of_apps_gitops_repo_path }}
    helm:
      values: |
